# Package examples

- `AuthenticationForm.vue` demonstrates a router-free `AuthLayout`, browser autocomplete, password visibility, and server validation.
- `ThemeModes.vue` demonstrates the default Security Academy palette in light and dark modes.

Consumers provide their own branding URLs; the package does not ship or assume company artwork.
